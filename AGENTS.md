# Arena Capability Benchmark — Agent Instructions

Use the deployed Worker URL's `/agents.md` endpoint for the final version because it fills in the correct API base URL automatically.

Example:

```text
https://YOUR-WORKER.YOUR-SUBDOMAIN.workers.dev/agents.md
```

The benchmark is stateless: start a run, complete all tasks, submit JSON, receive score.
